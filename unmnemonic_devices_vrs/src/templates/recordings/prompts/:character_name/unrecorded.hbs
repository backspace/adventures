<Response>
  {{#unless skip_message}}
    <Say>
      Will cycle through unrecorded prompts for {{character_name}}
    </Say>
  {{/unless}}
  <Redirect method="GET">{{redirect}}</Redirect>
</Response>
