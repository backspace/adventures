<Response>
  {{{lookup prompts "pure.prompt_sounds"}}}
  <Play>{{recording_url}}</Play>
  <Gather timeout="3" action="{{action}}" input="speech" hints="keep, rerecord">
    {{{lookup prompts "pure.prompt_action"}}}
  </Gather>
  {{> restart }}
</Response>
