<Response>
  {{#if maybe_encouraging}}
    {{{lookup prompts "pure.encouraging"}}}
    <Play>{{maybe_encouraging.url}}</Play>
  {{/if}}
  <!-- {{team.voicepass}} -->
  <Gather timeout="3" input="speech" hints="{{#each team.excerpts}}{{this}}, {{/each}}{{#each team.answers}}{{this}} {{/each}}">
    {{{lookup prompts "pure.excerpt_gather"}}}
  </Gather>
  {{> restart }}
</Response>
