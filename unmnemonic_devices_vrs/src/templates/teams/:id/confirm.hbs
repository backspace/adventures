<Response>
  <Gather timeout="3" input="speech" hints="yes, no">
    {{#if voicepass_url}}
      <Play>{{voicepass_url}}</Play>
    {{else}}
      <Say>
        {{voicepass}}
      </Say>
    {{/if}}
    {{{lookup prompts "pure.voicepass_confirm"}}}
  </Gather>
  {{> restart }}
</Response>
