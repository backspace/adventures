<Response>
  <Gather timeout="2" input="speech" hints="{{#each teams}}{{this.voicepass}}, {{/each}}">
    {{{lookup prompts "pure.voicepass"}}}
  </Gather>
  <!-- FIXME change to restart partial if prompt-loading works -->
  {{{lookup prompts "pure.silence"}}}
  <Redirect method="GET">/teams</Redirect>
</Response>
