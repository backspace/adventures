<h2>New team</h2>

<%= render "form.html", users: @users, team: @team, changeset: @changeset,
                        action: team_path(@conn, :create) %>

<%= link "Back", to: team_path(@conn, :index) %>
